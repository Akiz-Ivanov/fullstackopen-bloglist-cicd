{
  "name": "bloglist",
  "version": "1.0.0",
  "description": "Blog List app for FullStackOpen part 4",
  "author": "Akiz",
  "license": "ISC",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development node --watch index.js",
    "test": "cross-env NODE_ENV=test node --test",
    "start:test": "cross-env NODE_ENV=test node index.js",
    "lint": "eslint .",
    "build:ui": "cd ../frontend && npm install && npm run build && rm -rf ../backend/dist && cp -r dist ../backend/dist"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cross-env": "^7.0.3",
    "dotenv": "^17.2.0",
    "express": "^4.21.2",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^9.0.2",
    "lodash": "^4.17.21",
    "mongoose": "^8.16.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.1",
    "globals": "^17.6.0",
    "supertest": "^7.1.4"
  }
}